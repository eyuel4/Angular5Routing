import { IAppConfig } from './iapp.config';

export const AppConfig: IAppConfig = {
    routes: {
        home: 'home',
        signup: 'signup',
        login: 'login',
        error404: '404',
        notfound: 'not-found',
        courses: 'courses'
    },
    endpoints: {

    }
};